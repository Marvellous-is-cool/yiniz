<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin - Videos List</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
        margin: 0;
        padding: 20px;
      }
      h1 {
        text-align: center;
        color: #333;
      }
      .no-videos {
        text-align: center;
        padding: 50px;
        font-size: 18px;
        color: #777;
      }
      .video-card {
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        margin: 20px auto;
        padding: 20px;
        width: 80%;
      }
      .video-card h2 {
        margin: 0;
        font-size: 24px;
        color: #444;
      }
      .video-card video {
        display: block;
        margin: 10px auto;
        width: 100%;
        max-width: 500px;
      }
      .video-card p {
        font-size: 16px;
        margin: 10px 0;
      }
      .video-card form {
        margin-top: 20px;
        text-align: center;
      }
      .video-card form select,
      .video-card form button {
        padding: 10px;
        font-size: 16px;
        margin: 5px;
        cursor: pointer;
      }
    </style>
    <script
      async
      src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-4993685121054900"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <h1>Video Management</h1>

    <% if (videos && videos.length === 0) { %>
    <div class="no-videos">
      <p>No videos available at the moment.</p>
    </div>
    <% } else if (videos && videos.length > 0) { %> <%
    videos.forEach(function(video) { %>
    <div class="video-card">
      <h2><%= video.video_title %></h2>
      <video controls>
        <source src="<%= video.video_url %>" type="video/mp4" />
        Your browser does not support the video tag.
      </video>
      <p>User Name: <%= video.user_name %></p>
      <p>User Email: <%= video.user_email %></p>
      <p>Upload Time: <%= video.upload_time %></p>
      <p>Approval Status: <%= video.approved ? 'Approved' : 'Pending' %></p>

      <form
        action="/admin/games/roftfsbore/video/<%= video.id %>/approve"
        method="POST"
      >
        <label for="approved">Approve Video:</label>
        <select name="approved" id="approved">
          <option value="true">Approve</option>
          <option value="false">Reject</option>
        </select>
        <button type="submit">Submit</button>
      </form>
    </div>
    <% }); %> <% } %>
  </body>
</html>
